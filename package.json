{
  "name": "@webdesserts/scripts",
  "description": "A collection of cli scripts used to manage js projects",
  "private": true,
  "author": "Michael C Mullins",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "dev": "lerna run --parallel dev"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
